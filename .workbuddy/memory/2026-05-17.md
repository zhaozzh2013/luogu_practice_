# 2026-05-17

## 洛谷刷题软件 - 重大迭代

### 主要功能
- Flutter Windows 应用，Tokyo Night 暗色主题
- 左右分栏：左侧题库/题单列表，右侧题目详情
- 洛谷 API 爬虫（dart:io HttpClient，Clash 代理支持）
- VSCode 外联创建模板文件
- 题单功能（/training/list + /training/:id）

### 代码检测 Agent（新增）
- 新建 `lib/services/code_checker_service.dart`
- 支持：DeepSeek / 智谱GLM / Minimax / 腾讯混元 / ChatGPT
- AI 返回固定JSON：correct/score/feedback/issues/suggestions/counterExamples
- counterExamples 由AI自行生成，题目没说的问题（如int溢出）AI自行补充
- 集成到题目详情页：橙色「检测代码」按钮
- 新增API设置弹窗（设置→代码检测API）
- 结果弹窗展示：分数/对错/问题/建议/反例，支持切换AI提供商

### Bug修复
- 修复 IndexedStack bug：进入题单后中间栏消失（index=1时显示空的SizedBox）
- 修复方式：去掉IndexedStack，直接render _buildContent()

### Git
- 项目已初始化git并提交（commit f310baf）
- 待推送：等待用户提供GitHub用户名/repo URL
- flutter_markdown 渲染题目描述

### 技术要点
- `_fetchJson` 智能识别：纯 JSON（题目列表/详情）vs HTML 嵌入（题单列表）直接 json.decode OR 从 lentille-context 提取
- `flutter_markdown` 包用于 Markdown 渲染
- 题单列表 API: `GET /training/list?page=1` 返回 HTML 嵌入格式
- 题单详情 API: `GET /training/:id`
- 本地兜底：15 道经典题 + 5 个入门题单

### 已知问题
- 题单详情 `trainingProblems` 字段可能为空（API 限制）

### 本次重大 UI 重构（v1.2.1）
- **去掉左侧 56px 侧边栏**，改用顶部 Tab 导航（应用/题库/题单），全宽铺满
- 题单 API 确认返回**纯 JSON**（非 HTML），`fetchTrainingList` 可正常工作
- 做题界面顶部加入**折线统计图**（7天做题数据栏）
- 搜索框和题号跳转框分开：`_searchCtrl` 管搜索，`_jumpCtrl` 管跳转
- 题单页改为左右分栏：左侧题单列表 + 右侧题单内题目列表
- 应用页：运势卡片 + 快速数据 + 折线图 + 更新日志
- 题库页：搜索栏 + 难度筛选 + 题库列表，左侧宽度 340px

### 题单 API 修复（v1.2.2）
- `/training/list` 需要 `category=all` 参数才返回数据（已修复）
- 题单 API 字段映射：`provider.name` 为提供者，`provider.color` 为颜色
- `trainingProblems` 字段公开 API 不返回（需要登录 Cookie）
- **修复！** 题单详情改用普通 HTTP 请求（不带 `x-lentille-request`），从 HTML 的 `lentille-context` script 标签提取完整 JSON
- `lentille-context` 里的题单题目字段是 **`problems`**（不是 `trainingProblems`）
- `_extract` 方法重写：精准提取 `<script id="lentille-context" type="application/json">` 的内容
- 新增 `_fetchLentilleJson` 方法：不发 lentille header，获取完整 HTML 后提取数据
- 题单详情现在能正确显示全部题目列表
- **训练模式重磅升级**：
  - 点训练内题目后**保留训练上下文**，返回时无缝衔接（不再丢失训练进度）
  - 中间栏始终显示训练题目列表（带进度条）
  - 右侧顶部显示**训练上下文栏**：返回按钮 + 题单名 + 进度条（X/Y）+ 标记已做 + 下一题按钮
  - 题目序号变成绿色勾选图标（已做）/ 数字序号（未做）
  - 点「已做」按钮自动追踪进度；打开 VSCode 也自动标记为已做
  - 新增 `_activeTrainingProblemPid` 高亮当前查看的题目

### 用户偏好
- UI 偏好 DeepSeek App 风格：暗黑主题、模式选择器、底部输入栏
- 喜欢直观、一目了然的输入输出展示
